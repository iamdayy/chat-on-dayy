import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.chatondayy.app",
  appName: "ChatOnDayy",
  webDir: "dist",
  bundledWebRuntime: false,
};

export default config;
